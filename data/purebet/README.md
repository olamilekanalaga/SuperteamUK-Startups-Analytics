# Purebet data area

Future source extracts belong in immutable, date-stamped files below raw/. Staged decoder outputs belong below staged/; validated event and snapshot outputs belong below processed/.

This scaffold intentionally contains no synthetic or production rows. Synthetic fixtures exist only inside tests.